--- phase73/phase6/index.html
+++ phase74/phase6/index.html
@@ -2,7 +2,7 @@
 <html lang="es">
 <head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#090b10"><meta name="description" content="Cinco estados HMI, contexto SRS de NOAA 12975 y eventos GOES históricos independientes; Coronium estima SI actual, sin predicción de llamaradas. Exploración Unity y Needle en web y AR."><title>Auralis · Contexto científico NOAA 12975</title></head>
 <body data-phase="6">
-<header class="topbar"><a class="brand" href="./"><span class="brand-mark" aria-hidden="true">◉</span>AURALIS<span class="brand-divider"></span><span class="brand-sub">SOLAR LAB</span></a><span class="phase-tag">FASE 07.3 <span>NOAA 12975</span></span></header>
+<header class="topbar"><a class="brand" href="./"><span class="brand-mark" aria-hidden="true">◉</span>AURALIS<span class="brand-divider"></span><span class="brand-sub">SOLAR LAB</span></a><span class="phase-tag">FASE 07.4 <span>NOAA 12975</span></span></header>
 <main>
   <div class="page-heading"><div><p class="eyebrow">EXPLORADOR SOLAR</p><h1>El Sol y su contexto histórico.</h1></div><div class="observation-date"><span class="live-dot"></span><div><strong id="header-date">Cargando observación…</strong><span>Archivo SDO / HMI · registro UTC</span></div></div></div>
   <section class="temporal-panel" aria-label="Evolución temporal"><h2>24–30 marzo · cinco estados HMI de disco completo</h2><div class="science-legend" aria-label="Naturaleza de la información"><span>Observación real · HMI / SRS</span><span>Resultado de Coronium · SI</span><span>Recreación visual · Sol 3D</span><span>Evento histórico externo · GOES</span></div><div id="timeline"></div></section>
@@ -32,5 +32,5 @@
   <section class="ar-section" aria-labelledby="ar-title"><div class="ar-copy"><p class="eyebrow">DEL NAVEGADOR A TU ESPACIO</p><h2 id="ar-title">Explora este estado en AR.</h2><p id="capability" role="status">Comprobando compatibilidad del navegador…</p><p class="small">Abre AR en el menú del visor, busca una superficie y toca para colocar. El Sol mide 84 cm a escala 1×; es una escala de demostración.</p><label class="scale-label" for="scale">Escala del objeto <output id="scale-value">1,0×</output></label><input id="scale" type="range" min="0.4" max="1.5" step="0.1" value="1" disabled><p class="small">El gesto de escala de AR es independiente; comprueba el tamaño tras colocar.</p><p id="placement" class="small" role="status">Sin sesión AR. La exploración 3D está disponible.</p><a id="ios-launch" class="text-link" hidden>Abrir en Needle Go ↗</a><button id="quicklook" hidden>Quick Look · escala original</button><p class="small">iPhone: Safari → Needle Go (App Clip), sin WebXR nativo. Quick Look abre una instantánea de la superficie seleccionada, sin animación superficial, timeline, fundido ni controles web. WebXR y Needle Go usan el material web; su funcionamiento en este teléfono está pendiente de prueba física.</p></div><div class="qr-block"><canvas id="qr" aria-label="QR para abrir este estado en el teléfono"></canvas><strong>Continúa en tu teléfono</strong><a id="share-url"></a><p id="share-note" class="small"></p></div></section>
   <details class="validation"><summary>Prueba física y registro de este dispositivo</summary><div class="validation-grid"><div><ol><li>Escanea el QR. Recorre 24 → 25 → 28 → 29 → 30 y regresa: compara las regiones sobre el Sol y el magnetograma.</li><li>Mide 30 s en 3D. Entra en AR, concede cámara, coloca y explora delante y detrás.</li><li>En AR colocado, cambia las cinco fechas y capas; ajusta escala y comprueba frente y dorso; sal, vuelve a entrar y prueba bloquear/reanudar.</li><li>Anota seguimiento, legibilidad, demoras y calor. Descarga el registro.</li></ol><p class="small">Solo se registra lo que tú observes. No se envía información a un servidor. Needle Go o Quick Look pueden abrir otro contexto: registra sus resultados por separado.</p><button id="sample" disabled>Medir 30 s de render</button><pre id="sample-result" aria-live="polite">Sin medición.</pre></div><div class="human-fields"><label for="device">Teléfono y sistema</label><input id="device" placeholder="Modelo y versión del sistema"><label for="browser">Navegador y recorrido AR</label><input id="browser" placeholder="Versión; WebXR, Needle Go o Quick Look"><label for="outcome">Resultado de esta prueba</label><select id="outcome"><option value="not_tested">No probado</option><option value="passed">Funcionó en este dispositivo</option><option value="partial">Parcial</option><option value="failed">Falló</option></select><label for="observations">Observaciones</label><textarea id="observations" rows="4" placeholder="QR, materiales, capas, colocación, escala, deriva, reentrada y calor"></textarea><button id="download">Descargar registro</button></div></div></details>
   <details class="validation"><summary>Verificación técnica</summary><pre id="diagnostics">Esperando carga.</pre><button id="release" disabled>Liberar visor 3D</button><p class="small">Libera recursos de esta escena. La referencia 2D permanece disponible; Reintentar vuelve a cargar el visor.</p></details>
-</main><footer><span>AURALIS · GEMELO SOLAR / FASE 07.3</span><span>Observación · estimación · ilustración, distinguidas.</span></footer>
+</main><footer><span>AURALIS · GEMELO SOLAR / FASE 07.4</span><span>Observación · estimación · ilustración, distinguidas.</span></footer>
 <script type="module" src="/src/phase5/main.ts"></script><script type="module">if(import.meta.env.DEV && new URLSearchParams(location.search).has('capture')) import('/dev/phase72-capture.mjs');</script></body></html>
--- phase73/src/phase5/visual-materials.ts
+++ phase74/src/phase5/visual-materials.ts
@@ -15,22 +15,24 @@
     const solar=o.name==='IllustrativeSphere', scientific=o.name.startsWith('Layer_');
     if(temporal&&o.name==='SubtleCorona'){
       for(const material of Array.isArray(o.material)?o.material:[o.material]){
-        material.customProgramCacheKey=()=> 'auralis-corona-v73';
+        material.customProgramCacheKey=()=> 'auralis-corona-v74';
         material.onBeforeCompile=(shader:Parameters<Material['onBeforeCompile']>[0])=>{
           shader.uniforms.uStateFade=fade;
           shader.fragmentShader='uniform float uStateFade;\n'+shader.fragmentShader;
-          // Same exported Unity corona plane and texture. A short luminous rim
-          // plus a diffuse falloff replaces the previously faint, isolated wisps.
+          // Same Unity plane/texture, with a thinner falloff and broad irregularity.
+          // Decorative halo only; no atmospheric structure reconstructed from HMI.
           shader.fragmentShader=shader.fragmentShader.replace('#include <map_fragment>',`#ifdef USE_MAP
             vec4 bakedCorona=texture2D(map,vMapUv);
             float radius=length(vMapUv*2.0-1.0);
             float edge=max(0.0,radius-.70);
-            float outside=smoothstep(.694,.709,radius);
+            float outside=smoothstep(.700,.716,radius);
             float cutoff=1.0-smoothstep(.94,1.0,radius);
-            float innerGlow=exp(-edge*82.0);
-            float diffuseGlow=exp(-edge*12.0);
+            float innerGlow=exp(-edge*95.0);
+            float diffuseGlow=exp(-edge*14.0);
             diffuseColor.rgb*=mix(vec3(1.0,.17,.009),vec3(1.0,.52,.085),innerGlow);
-            diffuseColor.a*=(.52*innerGlow+.24*diffuseGlow+.20*bakedCorona.a)*outside*cutoff;
+            float angle=atan(vMapUv.y-.5,vMapUv.x-.5);
+            float irregular=.88+.07*sin(3.0*angle+.4)+.05*sin(5.0*angle-1.7);
+            diffuseColor.a*=(.42*innerGlow+.14*diffuseGlow+.12*bakedCorona.a)*outside*cutoff*irregular;
           #endif`);
           shader.fragmentShader=shader.fragmentShader.replace('#include <colorspace_fragment>','#include <colorspace_fragment>\ngl_FragColor.a*=uStateFade;');
         };
@@ -41,7 +43,7 @@
     for(const material of Array.isArray(o.material)?o.material:[o.material]) {
       if(solar&&temporal)decoration.value=(material as Material&{map:Texture}).map;
       material.toneMapped=false;
-      material.customProgramCacheKey=()=>solar?(temporal?'auralis-solar-motion-v73':'auralis-solar-polish-v1'):'auralis-data-display-'+o.name;
+      material.customProgramCacheKey=()=>solar?(temporal?'auralis-solar-motion-v74':'auralis-solar-polish-v1'):'auralis-data-display-'+o.name;
       material.onBeforeCompile=(shader:Parameters<Material['onBeforeCompile']>[0])=>{
         shader.uniforms.uIllustrationTime=time;shader.uniforms.uDisplayGain=gain;shader.uniforms.uStateFade=fade;
         shader.fragmentShader='uniform float uStateFade;\n'+shader.fragmentShader;
@@ -70,9 +72,9 @@
           #endif`);
           shader.fragmentShader=shader.fragmentShader.replace('#include <colorspace_fragment>',`#include <colorspace_fragment>
             float mu=clamp(dot(normalize(vSolarNormal),normalize(vSolarView)),0.0,1.0);
-            float limb=${temporal?'.38+.62*pow(mu,.56)':'.53+.47*pow(mu,.40)'};
+            float limb=${temporal?'.30+.70*pow(mu,.52)':'.53+.47*pow(mu,.40)'};
             gl_FragColor.rgb*=limb;
-            gl_FragColor.rgb+=${temporal?'vec3(.19,.055,.003)*pow(1.0-mu,15.0)':'vec3(.09,.035,.006)*pow(1.0-mu,9.0)'};
+            gl_FragColor.rgb+=${temporal?'vec3(.10,.028,.002)*pow(1.0-mu,18.0)':'vec3(.09,.035,.006)*pow(1.0-mu,9.0)'};
             gl_FragColor.rgb*=uStateFade;
           `);
         }else{
--- phase73/src/phase5/main.ts
+++ phase74/src/phase5/main.ts
@@ -1,3 +1,4 @@
+/// <reference types="vite/client" />
 import './style.css';
 import QRCode from 'qrcode';
 import { createSurfaceParticles, PARTICLE_COUNT } from '../phase6/surface-particles';
@@ -21,7 +22,7 @@
 const abort = new AbortController();
 const events:Record<string,unknown>[]=[];
 const samples:Record<string,unknown>[]=[];
-const diag:Record<string,unknown>={build:isTemporal?'phase73-v1':'phase5-visual-polish-v1',engine:'5.1.12',source:'waiting-for-real-unity',errors:[],physicalValidation:'not_tested',placementCount:0,layer:'solar'};
+const diag:Record<string,unknown>={build:isTemporal?'phase74-v1':'phase5-visual-polish-v1',engine:'5.1.12',source:'waiting-for-real-unity',errors:[],physicalValidation:'not_tested',placementCount:0,layer:'solar'};
 const ios=/iPad|iPhone|iPod/.test(navigator.userAgent)||(navigator.platform==='MacIntel'&&navigator.maxTouchPoints>1);
 let context:Context|undefined, object:Object3D|undefined, orbit:OrbitControls|undefined;
 let element:HTMLElement|undefined, removePlaced:(()=>void)|undefined;
@@ -101,6 +102,14 @@
 function resetSurface(){surfaceClock.reset(performance.now());plasma=false;visualTime=0;if(visual)visual.time.value=0;plasmaButton();record('surface-reset');}
 $('plasma').onclick=toggleSurface;plasmaButton();
 const surfaceReset=document.getElementById('surface-reset');if(surfaceReset)surfaceReset.onclick=resetSurface;
+// Local capture harness only: a reproducible presentation clock, never HMI time.
+// Vite removes this listener from production; normal web/AR controls are unchanged.
+if(import.meta.env.DEV&&isTemporal&&new URLSearchParams(location.search).has('capture'))listen(document,'auralis-capture-time',event=>{
+  const time=(event as CustomEvent<number>).detail;
+  if(!Number.isFinite(time)||time<0||time>30)return;
+  if(plasma)resetSurface();
+  surfaceClock.time=time;visualTime=time;if(visual)visual.time.value=time;
+});
 for(const button of Array.from(document.querySelectorAll<HTMLButtonElement>('[data-layer]')))button.onclick=()=>showLayer(button.dataset.layer!);
 function setScale(value:number){scale=clampScale(value);object?.scale.setScalar(scale);$('scale-value').textContent=scale.toFixed(1).replace('.',',')+'×';diag.scale=scale;}
 $('scale').oninput=e=>setScale(Number((e.target as HTMLInputElement).value));
@@ -190,7 +199,7 @@
   ctx.post_render_callbacks.push(()=>{
     const now=performance.now(),info=ctx.renderer.info;
     if(sample&&lastFrame!==info.render.frame){const mode=ctx.renderer.xr.isPresenting?'xr':'web3d';if(mode!==sample.mode)stopSample('interrupted-mode-change');else if(info.render.calls>0)sample.times.push(now);}lastFrame=info.render.frame;
-    if(now-lastStats>500){lastStats=now;Object.assign(diag,{status:'ready',layer,scale,autoRotate:automatic,illustrativeTime:visualTime,illustrativeMotion:plasma,...(isTemporal?{surfaceSeed:SURFACE_SEED,surfaceVisible,surfaceGuideState:data.state.id,surfaceGuideMaps:[visual?.guidePlus.value?.name,visual?.guideMinus.value?.name],surfaceShader:visual?.guidePlus.value?'anchored-hmi-v73':null}:{}),drawCalls:info.render.calls,triangles:info.render.triangles,renderFrames:info.render.frame,gpuTextures:info.memory.textures,gpuGeometries:info.memory.geometries,retainedEvents:events.length,pixelRatio:ctx.renderer.getPixelRatio(),cameraPosition:ctx.mainCamera.position.toArray(),cameraDistance:ctx.mainCamera.position.distanceTo(rootPosition),orbitEnabled:!!orbit?.controls?.enabled,canvasAttached:ctx.renderer.domElement.isConnected,engineElements:document.querySelectorAll('needle-engine').length});$('diagnostics').textContent=JSON.stringify(diag,null,2);}
+    if(now-lastStats>500){lastStats=now;Object.assign(diag,{status:'ready',layer,scale,autoRotate:automatic,illustrativeTime:visualTime,illustrativeMotion:plasma,...(isTemporal?{surfaceSeed:SURFACE_SEED,surfaceVisible,surfaceGuideState:data.state.id,surfaceGuideMaps:[visual?.guidePlus.value?.name,visual?.guideMinus.value?.name],surfaceShader:visual?.guidePlus.value?'anchored-hmi-v74':null}:{}),drawCalls:info.render.calls,triangles:info.render.triangles,renderFrames:info.render.frame,gpuTextures:info.memory.textures,gpuGeometries:info.memory.geometries,retainedEvents:events.length,pixelRatio:ctx.renderer.getPixelRatio(),cameraPosition:ctx.mainCamera.position.toArray(),cameraDistance:ctx.mainCamera.position.distanceTo(rootPosition),orbitEnabled:!!orbit?.controls?.enabled,canvasAttached:ctx.renderer.domElement.isConnected,engineElements:document.querySelectorAll('needle-engine').length});$('diagnostics').textContent=JSON.stringify(diag,null,2);}
   });
   temporal?.activate();scientific?.setEnabled(true);if(temporal&&visual){const texture=temporal.bundles[temporal.index].textures.get('solar')!;visual.solarFrom.value=texture;visual.solarTo.value=texture;}showLayer(layer);setScale(scale);setAutomatic(false);buttons(true);diag.source='unity-glb-loaded';diag.readyMs=Math.round(performance.now()-started);diag.exportHashVerified=true;diag.exportEvidence=evidence;
   $('load-status').textContent='Unity → Needle · escena y recursos verificados';clearTimeout(timer);$('retry').hidden=true;record('ready');
--- phase73/src/phase6/surface-shader.ts
+++ phase74/src/phase6/surface-shader.ts
@@ -22,23 +22,25 @@
 vec3 solarHash3(vec3 p) {
   return vec3(solarHash(p),solarHash(p+vec3(19.7,3.1,7.9)),solarHash(p+vec3(3.7,31.1,13.3)));
 }
-// Irregular cell interiors and narrow, filtered lanes. Object-space evaluation
-// has no UV seam or polar stretching. Sizes and motion are decorative, not km/s.
+// Continuous cellular detail: per-cell radii vary, but brightness never switches
+// abruptly to a nearest cell's ID. Smooth domain warping supplies larger clusters.
+// Filtering is applied after the nonlinear palette below, not just to the noise.
 float solarGranules(vec3 p,float time) {
-  vec3 tile=floor(p),f=fract(p);float nearest=9.0,second=9.0,identity=.5;
+  vec3 tile=floor(p),f=fract(p);float nearest=9.0,second=9.0;
   for(int z=-1;z<=1;z++)for(int y=-1;y<=1;y++)for(int x=-1;x<=1;x++){
     vec3 offset=vec3(float(x),float(y),float(z));
     vec3 h=solarHash3(tile+offset+vec3(uSurfaceSeed));
-    vec3 center=.5+(h-.5)*.82+.075*sin(time*mix(.16,.29,h.z)+h*6.2831853);
-    vec3 r=offset+center-f;float d=dot(r,r);
-    if(d<nearest)identity=h.x;
+    vec3 center=.5+(h-.5)*.82+.045*sin(time*mix(.11,.23,h.z)+h*6.2831853);
+    vec3 r=offset+center-f;float d=dot(r,r)/mix(.80,1.20,h.y);
     second=min(second,max(nearest,d));nearest=min(nearest,d);
   }
-  float footprint=length(fwidth(p));
-  float lane=smoothstep(.0,.34+footprint*.10,second-nearest);
-  float interior=exp(-3.3*nearest);
-  float cell=(.78*interior+.22*lane)*mix(.86,1.14,identity);
-  return mix(.46,cell,1.0-smoothstep(.65,1.65,footprint));
+  float lane=smoothstep(.0,.48,second-nearest);
+  float interior=exp(-2.8*nearest);
+  return (.90*interior+.10*lane)*mix(.90,1.10,solarNoise(p*.43));
+}
+vec3 solarPalette(float level) {
+  vec3 color=mix(vec3(.29,.039,.0025),vec3(1.0,.36,.024),smoothstep(.04,.57,level));
+  return mix(color,vec3(1.0,.79,.31),smoothstep(.54,.92,level));
 }
 // Cached sRGB textures encode nonnegative, linear channel magnitudes.
 float guideMagnitude(float c) { return c<=.0031308 ? c*12.92 : 1.055*pow(c,1.0/2.4)-.055; }
@@ -62,27 +64,29 @@
   vec3 transported=texture2D(uDecoration,vMapUv+flow*vec2(.0018,.0028)*pole*pole).rgb;
   float decorationLuma=dot(transported,vec3(.2126,.7152,.0722));
   float broad=solarNoise(n*13.0+vec3(3.1,7.9,uSurfaceSeed));
-  vec3 p=n*94.0+vec3(flow*.45,uSurfaceSeed*.17);
-  p+=.55*vec3(solarNoise(n*153.0),solarNoise(n.yzx*157.0+vec3(7.1)),solarNoise(n.zxy*149.0+vec3(19.3)));
+  vec3 p=n*88.0+vec3(flow*.45,uSurfaceSeed*.17);
+  p+=.95*vec3(solarNoise(n*23.0),solarNoise(n.yzx*29.0+vec3(7.1)),solarNoise(n.zxy*19.0+vec3(19.3)));
   float granules=solarGranules(p,uIllustrationTime);
   vec3 q=n.yzx*410.0+vec3(-flow.yx*.6,t*.10);
-  float fine=(solarNoise(q)-.5)*(1.0-smoothstep(.65,1.8,length(fwidth(q))));
+  float fine=(solarNoise(q)-.5)*(1.0-smoothstep(.35,1.15,length(fwidth(q))));
   // Three display scales: fine granules, intermediate mottling and restrained
   // original Unity decoration. No broad bright veins or global brightness pulse.
   float meso=solarNoise(n*33.0+vec3(flow*.35,11.7));
   vec3 detailP=n.zxy*203.0+vec3(7.1,flow*.35);
-  float detail=(solarNoise(detailP)-.5)*(1.0-smoothstep(.65,1.8,length(fwidth(detailP))));
-  float level=clamp(.10+.94*granules+.24*(meso-.5)+.14*detail+.10*fine,0.0,1.0);
-  // A display palette, not measured temperature or a color-calibrated image.
-  vec3 photosphere=mix(vec3(.29,.039,.0025),vec3(1.0,.36,.024),smoothstep(.04,.57,level));
-  photosphere=mix(photosphere,vec3(1.0,.79,.31),smoothstep(.54,.92,level));
+  float detail=(solarNoise(detailP)-.5)*(1.0-smoothstep(.35,1.15,length(fwidth(detailP))));
+  // Lower contrast at HMI cores/shoulders integrates decoration without changing
+  // the anchored attenuation or sampling neighboring observations.
+  float baseLevel=.50+.20*(meso-.5);
+  float level=clamp(baseLevel+.70*(granules-.46)*(1.0-.45*dark)+.09*detail+.05*fine,0.0,1.0);
+  // Preserve the exact gold/amber Phase 7.3 palette. Estimate the unresolved
+  // color average before blending out subpixel structure; no beige LOD fallback.
+  vec3 averageColor=.25*solarPalette(baseLevel-.12)+.50*solarPalette(baseLevel)+.25*solarPalette(baseLevel+.12);
+  float resolved=1.0-smoothstep(.38,1.20,length(fwidth(p)));
+  vec3 photosphere=mix(averageColor,solarPalette(level),resolved);
   float originalDetail=clamp(log2(max(.04,decorationLuma)/.34),-1.4,1.4);
-  photosphere*=.94+.19*(broad-.5)+.10*originalDetail;
-  // Very small screen-space relief cue on decorative granules only; no vertex
-  // displacement, new topography or inferred magnetic field geometry.
-  vec2 slope=vec2(dFdx(granules),dFdy(granules));
-  float relief=clamp(1.0+dot(slope,vec2(-.48,.68)),.90,1.10);
-  photosphere*=relief;
+  photosphere*=.94+.19*(broad-.5)+.07*originalDetail;
+  // Volume comes from the continuous limb law, not screen-pixel slope lighting.
+  // No normals, vertices or reconstructed surface heights are introduced.
   // Keep exactly the same scalar HMI attenuation as Phase 7.2. Warming the
   // shoulders improves integration without moving, enlarging or lifting cores.
   float attenuation=pow(clamp(anchoredRatio.r,.018,1.35),.96);
@@ -91,5 +95,5 @@
   photosphere=mix(photosphere,polarityTint,guide*.13);
   photosphere+=vec3(.15,.055,.003)*shoulder*(.75+.25*meso);
   diffuseColor.rgb=photosphere*attenuation;
-  diffuseColor.rgb*=1.0+.08*detail*(1.0-.75*dark)*(1.0-.5*guide);
+  diffuseColor.rgb*=1.0+.04*detail*(1.0-.75*dark)*(1.0-.5*guide);
 `;
--- phase73/dev/phase72-capture.mjs
+++ phase74/dev/phase72-capture.mjs
@@ -1,5 +1,6 @@
 // Local-only evidence harness, imported only by Vite DEV + ?capture.
-// Uses the rendered canvas and real controls; never mutates science or camera.
+// Uses the rendered canvas, fixed evidence cameras and a DEV-only presentation clock.
+// No science, HMI time, source textures or production clock behavior is changed.
 import {Context,OrbitControls} from '@needle-tools/engine';
 import {Vector3} from 'three';
 // WebGL discards its drawing buffer outside the frame. Read after a real render
@@ -29,17 +30,21 @@
  const science=()=>JSON.parse(document.querySelector('#diagnostics').textContent);
  const before=science(),snapshots=[];
  const frozenFrames=[];
+ const setTime=time=>document.dispatchEvent(new CustomEvent('auralis-capture-time',{detail:time}));
+ let animationFrame=0;
+ const epoch=performance.now();
+ const tick=now=>{setTime(Math.min(8,Math.max(0,(now-epoch)/1000)));if(now-epoch<8000)animationFrame=requestAnimationFrame(tick);};
  const grabFrozen=async()=>frozenFrames.push({time:performance.now(),png:await captureFrame(),science:science()});
  const interval=setInterval(()=>snapshots.push(science()),500);
  recorder.ondataavailable=e=>{if(e.data.size)chunks.push(e.data);};
  recorder.onstop=async()=>{
    clearInterval(interval);stream.getTracks().forEach(t=>t.stop());
    const blob=new Blob(chunks,{type:recorder.mimeType});const reader=new FileReader();
-   reader.onload=()=>{output.value=JSON.stringify({video:reader.result,before,after:science(),snapshots,frozenFrames,note:'9 s, fixed selected camera/date. 0–8 s running from t=0 at normal speed; 8–9 s paused. No physical AR.'});button.disabled=false;button.textContent='Grabar comparación 9 s';};reader.readAsDataURL(blob);
+   reader.onload=()=>{output.value=JSON.stringify({video:reader.result,before,after:science(),snapshots,frozenFrames,note:'9 s, fixed camera/date/resolution. DEV presentation clock runs t=0 to exactly 8 s at 1x, then holds t=8 for 1 s. Same clock schedule on both versions. Recorded frame cadence is browser-dependent. Production controls tested separately. No physical AR.'});button.disabled=false;button.textContent='Grabar comparación 9 s';};reader.readAsDataURL(blob);
  };
  recorder.start();
- document.querySelector('#plasma').click();
- setTimeout(()=>document.querySelector('#plasma').click(),8000);
+ setTime(0);animationFrame=requestAnimationFrame(tick);
+ setTimeout(()=>{cancelAnimationFrame(animationFrame);setTime(8);},8000);
  setTimeout(grabFrozen,8400);setTimeout(grabFrozen,8900);
  setTimeout(()=>recorder.stop(),9000);
 };
